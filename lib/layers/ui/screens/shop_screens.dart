import 'package:flutter/material.dart';

class ShopScreens extends StatelessWidget {
  const ShopScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ShopScreens'),
    );
  }
}